# История запросов по проекту

Ниже собрана история запросов по проекту из текущей переписки. Это реконструкция по ходу диалога: запросы сохранены в логическом и примерно хронологическом порядке, включая уточнения, исправления и связанные эксплуатационные вопросы.

## 1. База данных, нагрузка и диагностика

1. Анализ `idle in transaction` в PostgreSQL и вопрос «что по второму пункту».
2. Объяснение, что такое `audit logs`, зачем они нужны и как снизить их влияние.
3. Оптимизация lookup-запросов по школам.
4. Поиск причин большого количества запросов `lookup`, даже когда нет активной регистрации пользователей.
5. Требование не дергать `lookup` при открытии страницы, а вызывать только по вводу.
6. Анализ метрик из файла `metrics_2026-02-02_19-34-03.log` под реальной нагрузкой.
7. Анализ множества `401 Unauthorized` при прохождении олимпиады и вопрос, нагружают ли эти запросы БД и создают ли очередь.
8. Объяснение текущего rate limit для сохранения ответов:
   - `ANSWERS_RL_LIMIT`
   - `ANSWERS_RL_WINDOW_SEC`
   - `SUBMIT_LOCK_TTL_SEC`
   - лимиты логина, регистрации, подтверждения, сброса пароля и смены пароля.
9. Разбор кейсов, когда пользователь видит «Не удалось сохранить» при действующей попытке.
10. Команды просмотра логов `docker compose logs -f --tail=200 api | grep "/attempts/"`.
11. Уточнение, почему при просмотре логов видны не все строки.
12. Проверка worker/Celery по логам.
13. Обсуждение `requests`/`urllib3` warnings и runtime-warning в API-контейнере.

## 2. Git, окружение сервера и эксплуатационные команды

1. Как узнать, какие изменения есть на сервере в git.
2. Анализ вывода `git status -sb` с большим количеством `node_modules`, `dist`, `.env`, `backups` и других артефактов.
3. Как заблокировать изменение `.env` через `git commit`.
4. Как заблокировать изменение `.env` через `git pull`.
5. Проверка того, что `.env` не находится под `git ls-files`.
6. Как сделать backup `.env`.
7. Какие команды на сервере использовать для применения изменений.
8. Как сделать backup всей БД на сервере.
9. Как копировать каталоги/файлы между локальной машиной и сервером.
10. Как убирать лишние файлы из git перед публикацией.
11. Команды применения изменений после commit.
12. Конфликт `git pull` из-за локальных изменений в `regrade_task49.sh`, `.gitignore` и других файлов.
13. Вопрос о merge-конфликте в `.gitignore` и решение оставить только локальный вариант.

## 3. Личный кабинет пользователя и frontend-ограничения

1. Как временно заблокировать изменение поля `grade` в личном кабинете только на frontend.
2. Просьба указать, куда именно добавить `disabled`.
3. Скрытие кнопки «Пробная олимпиада» в разделе «Начать олимпиаду» на главной.
4. Временная блокировка редактирования `grade` без backend-изменений.
5. Показ окна «Олимпиада в процессе» при повторном входе пользователя в аккаунт, если есть активная попытка:
   - текст с названием олимпиады,
   - кнопки «Продолжить» и «Отмена»,
   - переход на активную попытку.
6. Индикация правильных/неправильных ответов в личном кабинете пользователя при просмотре попытки.
7. Отображение стоимости каждого задания в попытке пользователя в личном кабинете.
8. Изменение сообщения при отсутствии диплома с «Диплом не найден. Обратитесь в службу технической поддержки» на «Диплом будет загружен позже».

## 4. Аутентификация, сессии и autosave в олимпиаде

1. Почему во время прохождения олимпиады через 30 минут пользователь становится неавторизован и в верхнем меню видит «гость».
2. Каков срок жизни сессии пользователя.
3. Что будет, если увеличить `access token` до 60 минут, и какие это несет риски.
4. Выбросит ли пользователя через 30 минут, если олимпиада идет 60 минут.
5. Рекомендации, как избежать потери авторизации во время прохождения.
6. Анализ уже внесенных ранее изменений на эту тему.
7. Проверка конфигурации `JWT_ACCESS_TTL_MIN=45`.
8. Подготовка полного набора правок для предотвращения выхода пользователя из авторизации в разных вариантах.
9. Идея и обсуждение silent-refresh.
10. Откат изменений, связанных с silent-refresh:
    - каждые 20 минут `auth.refresh`
    - `clearOnFail`
    - обновление сессии через `setSession`.
11. Предложение:
    - при `401` сначала пробовать refresh,
    - только при неуспехе делать signOut и останавливать autosave.
12. Обсуждение автоматического refresh каждые 45 минут при access token 60 минут.
13. Объяснение, зачем вообще предлагалось менять refresh timer.
14. Логи с `GET /auth/me`, `GET /attempts/results/my 401`, `POST /auth/refresh 422`.
15. Рекомендации по предотвращению ситуаций, когда refresh падает и пользователь теряет сессию.
16. Просьба выполнить меры по максимальному предотвращению таких случаев.
17. Отказ от модалки «Сессия истекла» и замена на немедленный вход; при выходе из активной попытки должен быть возврат в нее.

## 5. Олимпиада: таймер, дедлайны и завершение попыток

1. Анализ кейса: пользователь зашел в 21:50, олимпиада доступна до 22:00 — что произойдет с попыткой в 22:00.
2. Реализация жесткого backend-ограничения: `deadline_at = min(started_at + duration_sec, olympiad.available_to)`.
3. Подтверждение логики, что если пользователь начал в 21:30 при длительности 50 минут, а доступность до 22:00, то таймер должен показывать 30 минут.
4. Вопрос, нужно ли перезапускать docker-контейнер `app` после правок.
5. Анализ кейса мгновенного `submit` и таймера `00:00` при старте попытки.
6. Почему пользователь мог сразу уйти в `submit`.
7. Как избежать ошибок таймера на клиенте.
8. Встраивание offset от сервера, чтобы избежать моментального завершения по таймеру.
9. Подтверждение, что server offset помогает против ложного мгновенного завершения.
10. Блокировка других кнопок и закрытия окна на 3 секунды или до завершения запроса при нажатии «Завершить» в модальном окне подтверждения.
11. Добавление предупреждающей модалки при входе в олимпиаду, если `start + duration > deadline_at`.

## 6. Олимпиада: expired-попытки, оценивание и пересчет результатов

1. Проблема: у пользователей, у которых попытка завершилась по истечению времени, не отображается дата попытки в личном кабинете и результат показывает `0%`.
2. Вопрос, исправит ли новый код уже записанные в БД попытки, если ответы сохранены.
3. Исправление backend-логики:
   - при `expired` попытке запускать оценивание,
   - заполнять `score_total`, `score_max`, `passed`, `graded_at`,
   - обновлять `attempt_task_grades`.
4. Добавление аналогичного пересчета для случая авто-expire в `get_attempt_view()`.
5. Написание скрипта для переоценки всех `expired` попыток без оценок.
6. Подтверждение, что скрипт:
   - находит `expired` с `graded_at IS NULL` или `score_max=0`,
   - пересчитывает баллы,
   - выставляет дату прохождения,
   - обновляет `attempt_task_grades`.
7. Жалоба, что даже после правок для `expired` не засчитывается прохождение и не видны правильные/неправильные ответы.

## 7. Олимпиада: markdown и отображение текста заданий

1. Проблема: в режиме прохождения олимпиады markdown не отображается, `**жирный**` виден со звездочками.
2. Просьба заменить вывод `activeTask.content` на безопасный markdown-парсер, как в админке.
3. Расширение markdown-рендера на другие страницы с отображением условий.

## 8. Олимпиада: валидация кратких ответов

1. Валидация при вводе ответа на задание с кратким ответом:
   - предупреждать, если для `int` введено не целое число.
2. Не блокировать сохранение, только предупреждать.
3. Затем сделать строгую валидацию:
   - для `int` нельзя сохранить ответ и перейти дальше, если это не целое число;
   - для `float` нельзя сохранить ответ, если это не число или не десятичная дробь.
4. Сообщения:
   - `В ответ можно указать только целое число`
   - `В ответ можно указать только целое число или десятичную дробь`
5. Повторная жалоба, что пользователь все еще может вводить любой текст для `int` и он сохраняется.
6. Откат изменений, так как проект в проде и backend сейчас нельзя трогать.
7. Отдельная frontend-задача: placeholder’ы в поле краткого ответа:
   - `только целое число` для `int`
   - `только число` для `float`
   - `ответ` для `text`.
8. Позже — backend-правки:
   - `AttemptView` должен передавать `subtype` для `short_text`,
   - `upsert_answer` должен строго валидировать `int` и `float` по regex и отдавать `422`.

## 9. Результаты, задания, попытки и админ-панель

1. В админке в разделе результатов при выборе олимпиады показывать:
   - количество открытых попыток,
   - количество всех попыток по олимпиаде.
2. Добавить `user_id` в таблицу результатов попыток в админке.
3. Добавить `gender` и `teachers` пользователя в таблицу результатов попыток в админке.
4. В админке в разделе пользователей:
   - разместить формы редактирования и формы поиска в 4 ряда без горизонтального скролла,
   - ширина форм 100 px, потом 300 px,
   - затем уложить все в условный grid `4 в ряд`.
5. Таблицу с пользователями при этом не трогать.
6. В админке в разделе олимпиад при создании не отображались все задания и поиск работал только среди видимых — сделать показ всех доступных заданий и поиск по всему пуллу.
7. В админке в предпросмотре олимпиады показать:
   - количество заданий,
   - общее количество баллов,
   - баллы по каждому заданию.
8. Выгрузка в CSV в админке с корректной кириллицей в Excel.
9. В разделе отчетов админки добавить статистику скачивания дипломов.
10. Создание отдельной страницы результатов и архива заданий:
    - сначала `ResultsArchivePage`,
    - затем разделение на `ArchivePage` и `ResultPage`.

## 10. Дипломы участников

1. Рекомендации по именованию дипломов:
   - `attempt_<attempt_id>.jpg`
2. Рекомендации по хранению дипломов в MinIO.
3. Рекомендации по выдаче диплома через endpoint `/api/v1/attempts/{id}/diploma`.
4. Скрипт для массовой загрузки дипломов в MinIO.
5. Команды копирования дипломов с Windows на сервер без `rsync`.
6. Разбор ошибок при загрузке в MinIO:
   - `maximum recursion depth exceeded`
   - ошибки endpoint
   - `404 Not Found` при открытии диплома
7. Настройка backend-endpoint диплома без изменения `.env`.
8. Изменение поведения кнопки диплома: открывать диплом в новом окне браузера.
9. Затем сделать как с `poster` на главной, чтобы не блокировалось всплывающее окно браузером.
10. Если диплома нет — показывать сообщение пользователю.

## 11. Благодарности и сертификаты учителей

1. Рекомендации по реализации раздела сертификатов учителей в личном кабинете:
   - если за сезон может быть несколько сертификатов.
2. Выбор схемы имени файла:
   - оставить `user_id`,
   - добавить `season`,
   - добавить `seq`.
3. Вариант имени:
   - `certificate_<user_id>_<YYYY>_<YYYY+1>_<seq>.jpg`
4. Привязка `seq` к отдельному названию сертификата в UI.
5. Реализация backend для сертификатов учителей.
6. Реализация frontend раздела «Благодарности и сертификаты».
7. Скрипт загрузки сертификатов учителей в MinIO.
8. Исправление ошибок загрузки сертификатов:
   - endpoint,
   - `.png` -> `.jpg`,
   - regex имени,
   - `RecursionError`.
9. Диагностика, почему сертификаты не отображаются у учителя с `id=17`.
10. Проверки:
    - роль `teacher`,
    - наличие файлов в MinIO,
    - endpoint `/api/v1/teacher/certificates`.
11. Исправление backend/API, чтобы сертификаты загружались сразу в личном кабинете, без задержки.

## 12. Массовые SQL/CSV-операции и пользовательские данные

1. Скрипт для массового обновления `country`, `city`, `school` пользователей из CSV.
2. Обсуждение разделителя CSV и dry-run режима.
3. Диагностика ошибки `\copy: parse error at end of line`.
4. Проверка допустимой длины поля `school` в БД.
5. Массовый импорт результатов очного тура:
   - файлы `math_final_attempts.csv`, `inf_final_attempts.csv`,
   - олимпиады `52` и `53`,
   - создание попыток в `attempts`,
   - вопросы по duration, id, отсутствию попыток на сервере.
6. Скрипт `import_final_round_attempts.sh` для server-side импорта.
7. Исправления dry-run скрипта:
   - `\copy`,
   - shell PID в `DO $$`,
   - битые timezone suffix в CSV (`+16`, `+17` и т.д.),
   - нормализация дат.

## 13. Переоценка задач и исправление ошибок в проверке

1. Исправление задачи `id=49`, чтобы правильными считались ответы `B` и `C`.
2. Переоценка попыток в диапазоне `2299..4183`.
3. Ошибка в SQL-скрипте пересчета и переход к `sh`-скрипту `regrade_task49.sh`.
4. Ситуация, когда скрипт пометил ответ зеленым, но не пересчитал баллы и проценты.
5. Исправление конкретной попытки `5538` пользователя `1788` по задаче `75`.
6. Серия скриптов для задач `50` и `51`, где ответ мог содержать число или словесный вариант:
   - «три»
   - «четыре»
7. Позже расширение логики на задачи:
   - `39, 40, 41, 42, 44, 53, 54, 55, 56, 57, 58, 59, 60, 61, 63, 66, 67, 68, 71`
   - с учетом вхождения чисел из `payload.expected` и словесных вариантов ответа.
8. Выгрузка CSV-отчета по результатам работы скрипта.
9. Экспорт сводных CSV по спискам попыток и затем по всем существующим попыткам.
10. Скрипт для задачи `37` со словами `девятая`, `девять`, `девятой`.

## 14. PDF, экспорт и олимпиадные документы

1. Возможность в админке скачивать PDF-версию олимпиады с настройкой:
   - описание олимпиады
   - название заданий
   - тип задания / тип ответа
   - правильный ответ
2. Уточнение итогового формата PDF:
   - название олимпиады
   - классы
   - число заданий и максимум баллов
   - описание (опционально)
   - далее задания со структурой.
3. Исправление шрифтов в PDF (`olympiad_51.pdf`), так как кириллица не отображалась.
4. Использование другого шрифта, который не надо устанавливать.
5. Обязательное добавление вариантов ответов в PDF для заданий с выбором.
6. Исправление отображения правильного ответа в PDF для single/multi choice:
   - сначала внутри списка,
   - затем отдельным блоком «Правильный ответ».

## 15. Главная страница и публичные страницы сайта

1. Изменение главной страницы:
   - блок «Тестирование по коду» после «Об олимпиаде»
   - FAQ скрыть под плашкой
   - перед «Об олимпиаде» добавить блок объявления с `announcement.md` и виджетом из `clock_vidget.md`
   - не трогать `home-hero-panel`, который уже скрыт.
2. Проверка, сохранились ли изменения в `HomePage.tsx`.
3. Увеличение `clock_vidget` в 2 раза.
4. Удаление markdown-рендера и вставка кода объявления/виджета напрямую в `HomePage.tsx`.
5. Создание отдельной страницы `ResultsArchivePage` и последующее разделение на:
   - `ArchivePage`
   - `ResultPage`
6. Кнопки PDF на голубом фоне с иконкой PDF.
7. Заменить `+` в плашках на стрелочки, как в FAQ.
8. Проверка существования PDF-файлов перед показом кнопки.
9. Сначала хотели модалку «Информация будет размещена позже», потом решили скрывать отсутствующие кнопки, а затем показывать вместо них текст «Информация будет добавлена позже».
10. Добавление `statistic.png` на страницу результатов.
11. Убрать рамку и тень у картинки статистики, выровнять ее и подпись по центру.
12. Позже — переработка `ResultPage`:
   - сначала показать PDF целиком как HTML через изображения страниц,
   - затем показать только диаграммы как изображения,
   - а текст оформить в HTML через `<h3>` и `<p>`.
13. Отдельные требования по верстке `ResultPage`:
   - текст на 80% ширины страницы,
   - диаграммы сначала на 40%, потом на 60%,
   - первая диаграмма по центру,
   - абзацы отдельным стилизованным классом,
   - выравнивание по ширине,
   - отступ первой строки на 4 пробела.
14. Удаление временных файлов после локального рендера PDF.

## 16. Массовая email-рассылка

1. Использовать служебный транзакционный email для массовой рассылки.
2. Исходные файлы:
   - `emails_final.csv`
   - `email_text.md`
   - тема письма `Подтверждение участия в очном туре`
3. Нужен скрипт для запуска из командной строки на сервере.
4. Затем запуск через `docker compose exec api ...`.
5. Исправление проблемы с импортом `httpx` на хосте.
6. Перенос скрипта в `backend/app/scripts/`.
7. Диагностика отсутствия `email_text.md` и `emails_final.csv` внутри контейнера.

## 17. Объявления, кампании и сегментация пользователей

1. Рекомендации, как показывать некоторым ученикам специальное приглашение или сообщение «Спасибо за участие».
2. Обсуждение отдельной таблицы вместо расширения `users`.
3. Уточнение структуры:
   - 8 групп пользователей,
   - затем 22 группы,
   - возможность показывать одно или два объявления,
   - разные сообщения по математике и информатике,
   - настройка времени публикации из админки.
4. Предложение структуры с отдельной сущностью кампаний объявлений и группового таргетинга.
5. Реализация backend:
   - кампании объявлений
   - группы
   - fallback-объявления
   - импорт CSV
   - endpoint `/users/me/announcements`
6. Merge Alembic heads.
7. Реализация frontend:
   - блок объявлений в личном кабинете пользователя
   - админка для управления объявлениями
8. Пояснение, что `Fallback-объявление` показывается тем, кого нет в списках групп.
9. Исправление ошибки в кабинете, когда из-за `undefined body` ломался markdown при объявлениях.
10. Вопрос, как сделать ссылки в markdown кликабельными и как стилизовать их как обычные гиперссылки.
11. Изменение `cabinet.css` для стилизации ссылок в объявлениях.

## 18. Прочие точечные запросы и команды

1. Команда удаления попытки по `id`.
2. Объяснение, почему при удалении `attempt_answers` удалилось, например, только 2 строки.
3. Как закомментировать строку в TypeScript/TSX.
4. Команда backup всей БД `pg_dump`.
5. Команда копирования каталога `diploma_new` на сервер.
6. Вопрос, можно ли через `upload_diplomas_minio.py` заменить старые дипломы новыми.
7. Подсчет строк кода:
   - весь проект,
   - только `py` без пустых строк и комментариев,
   - только `backend/app`,
   - затем в `frontend/apps`.

## 19. Итоговые публичные страницы результатов и архива

1. Исходно была одна страница `ResultsArchivePage`, вызываемая по пункту меню «Архив заданий».
2. Затем запрос на создание двух страниц:
   - `ArchivePage`
   - `ResultPage`
3. `ArchivePage` должна сохранить текущее содержимое старой страницы.
4. `ResultPage` должна показывать информацию из `final_statistic.pdf`.
5. Сначала PDF был встроен через изображения целых страниц.
6. Затем потребовалось:
   - рендерить только диаграммы,
   - текст перевести в HTML через `<h3>` и `<p>`,
   - задать отдельные стили текста,
   - выровнять текст по ширине,
   - контролировать ширину текстового блока и диаграмм,
   - добавить отступ первой строки.

## 20. Текущее состояние по результатам этой истории

На момент составления этого файла в проекте обсуждались и/или реализовывались:

1. Множественные исправления backend-логики попыток, дедлайнов, оценивания и экспирации.
2. Много правок frontend для кабинета, олимпиадной страницы, главной страницы и публичных разделов результатов.
3. Скрипты для массовых операций:
   - переоценка задач,
   - импорт CSV,
   - загрузка дипломов и сертификатов,
   - массовая email-рассылка,
   - экспорт отчетов.
4. Интеграции с MinIO/S3, PDF-генерацией и управлением пользовательскими объявлениями.

---

Если понадобится, этот файл можно расширить в следующих форматах:

1. Как чистую временную шкалу по датам и задачам.
2. Как changelog по подсистемам (`backend`, `frontend`, `infra`, `scripts`).
3. Как список всех созданных и измененных скриптов/страниц/эндпоинтов.
